# Frontend environment variables
# NEXT_PUBLIC_* is inlined at BUILD time by Next.js — it must be set when the
# image / app is built, not just at runtime.
#   Local dev:   http://localhost:3001
#   Production:  https://lato.example.com
NEXT_PUBLIC_API_URL=http://localhost:3001
